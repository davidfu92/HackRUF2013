$.get("http://162.243.41.64/HackRUF2013/database.php?location=hi").done(function(data) `{
	    //alert("Data: " + data + "\nStatus: " + status);
		navigator.geolocation.getCurrentPosition(lookUPPosition);
});
function lookUPPositionPosition(position)
{
	if(Math.abs(data[0]['Lat'] - position.coords.latitude)<.000001) {
		$.get("http://162.243.41.64/HackRUF2013/database.php?URL=",data).done(function(data) `{
		navigator.geolocation.getCurrentPosition(lookUPPosition);
});
f(Math.abs(data[0]['Long'] - position.coords.longitude) {
		}
	}
}
function onVisit() {
Chrome.history.onVisited.addListener(checkHistory(HistoryItem i));
}
function checkHistory(HisotryItem i) {
	$.get("http://162.243.41.64/HackRUF2013/database.php?URL=",i.url).done(function(data) {
	
	});
}

